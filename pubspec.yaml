name: fancy_bottom_navigation_plus
description: An animated Bottom Navigation Bar for Flutter apps, icon animates into place, colors are customisable..
version: 0.1.0
homepage: https://github.com/jinosh05/fancy_bottom_navigation_plus

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  vector_math: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
